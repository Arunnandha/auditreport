{
  "name": "audit_report",
  "version": "1.0.0",
  "scripts": {
    "client": "cd CLIENT && npm start",
    "server": "cd SERVICE && nodemon server.js",
    "dev": "concurrently --kill-others-on-fail \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^4.0.1"
  }
}
